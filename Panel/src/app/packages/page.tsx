import { SectionPage } from "../../components/SectionPage";

export default function PackagesPage() {
  return (
    <SectionPage
      title="Packages"
      active="packages"
      blurb="Manage hosting packages and account resource limits."
    />
  );
}
