import { SectionPage } from "../../components/SectionPage";

export default function PluginsPage() {
  return (
    <SectionPage
      title="Plugins"
      active="plugins"
      blurb="Installed plugins and the CPN Plugin Store (served by cpn-installer on the panel port)."
    />
  );
}
